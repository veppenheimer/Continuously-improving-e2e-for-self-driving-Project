# 陌生环境泛化能力提升调整说明

## 1. 背景与核心问题

当前项目的目标是端到端自动驾驶转向角预测，输入为单目图像，输出为车辆控制所需的转向角。根据前期实验结果可以看到：

- 原始回归模型在随机划分测试集上表现尚可。
- 但在陌生环境、强光照变化、局部曝光变化、不同场地风格下，预测误差会明显放大。
- 分类残差模型虽然在部分测试集上表现较好，但在强风格扰动下也没有稳定超越原始回归模型。
- `Net_improve` 的网络结构增强并没有带来稳定收益，甚至在部分外部测试中表现更差。

这说明当前瓶颈不是简单地“模型不够复杂”或“数据增强不够多”，而是训练目标、输入契约、增强策略、选模指标和推理流程之间没有围绕“陌生环境泛化”统一设计。

因此本次调整的目标不是单纯提高随机测试集分数，而是尽可能提高模型在实际应用场景中的稳定性，尤其是面对陌生环境和光照/曝光/风格变化时的泛化能力。

## 2. 总体调整思路

本次调整围绕以下原则展开：

1. 训练和推理必须一致。

   模型训练时看到的输入格式、颜色空间、尺寸和实际部署推理时必须一致，避免因为预处理差异造成额外误差。

2. 优先增强对环境风格变化的鲁棒性。

   当前陌生环境失败主要体现为光照、亮度、对比度、局部曝光和图像风格变化带来的域偏移，因此增强重点放在 photometric / image-quality 变化上，而不是几何变化。

3. 禁止破坏转向语义的增强。

   不使用翻转、裁剪、旋转、仿射、透视、形变、遮挡等可能导致图像与转向角标签不匹配的增强。

4. 用 `val_stress` 选择模型。

   普通随机验证集不能充分反映陌生环境鲁棒性，因此新增强风格验证集 `val_stress`，用它作为主要选模指标。

5. 保留两条主线，同时控制部署复杂度。

   保留回归主线和分类残差主线，但都使用轻量预训练骨干，不显著增加 Jetson Nano 部署负担。

## 3. 统一训练与推理输入契约

### 3.1 统一默认输入流程

本次将训练、Web 推理、联调脚本和 TensorRT 推理默认输入统一为：

```text
整张图像 -> HSV -> Resize(120, 160) -> Tensor
```

默认不使用 ROI：

```text
ROI = False
```

这样做的原因是：

- 之前联调脚本默认使用 ROI，而训练和部分推理链使用整图，容易造成训练/测试/部署不一致。
- ROI 是否有效和具体场地强相关，作为正式默认策略风险较高。
- 当前阶段更希望先建立一个稳定、可复现的全图基线。

ROI 仍然保留，但只作为诊断开关使用。例如分析道路下半部分是否更可靠时，可以手动开启。

### 3.2 新增共享预处理模块

新增文件：

```text
E:\桌面\项目\steering_preprocess.py
```

其中定义了统一的 `PreprocessConfig`：

```python
PreprocessConfig(
    color_space="hsv",
    input_size=(120, 160),
    use_roi=False,
)
```

以下链路已经接入该预处理契约：

- `e2e_self-driving/Net/train.py`
- `e2e_competition/Net_class/train.py`
- `e2e_competition/Net_improve/train.py`
- `e2e_self-driving/Net_Web/app/services/inference.py`
- `joint_infer_compare.py`
- `joint_infer_dataset_compare.py`
- `e2e_self-driving/trtCrun.py`

这样可以减少“训练时一套预处理、推理时另一套预处理”的隐性误差。

## 4. 数据增强策略调整

### 4.1 从“增强训练集规模”转向“增强泛化鲁棒性”

之前我们尝试过离线增强和强风格增强测试，但结果显示：

- 单纯扩充数据量不一定能提升真实泛化。
- 如果增强分布和训练/验证/选模目标没有配合好，模型可能仍然只学到当前场地特征。
- 强扰动测试集可以暴露模型脆弱性，但不能简单等同于把所有训练图都强增强。

因此本次调整不再以“尽量生成更多增强图”为核心，而是采用在线混合增强，并新增 `val_stress` 用于模型筛选。

### 4.2 新增共享增强模块

新增文件：

```text
E:\桌面\项目\steering_augmentations.py
```

训练增强默认比例为：

```text
50% clean 原图风格
30% moderate photometric 增强
20% strong style 增强
```

默认配置：

```text
VENET_STYLE_MIX_RATIO=0.5,0.3,0.2
```

这样设计的原因是：

- 保留一半干净样本，避免训练分布过度偏移。
- 中等增强覆盖常见光照变化。
- 强风格增强提高对极端曝光、亮度和对比度变化的适应能力。

### 4.3 只使用安全增强

允许的增强主要包括：

- 亮度变化
- 对比度变化
- Gamma 变化
- 小幅颜色扰动
- CLAHE
- 局部曝光 / 局部阴影
- 轻微噪声
- 轻微模糊
- 轻微压缩
- 轻微锐化

明确不使用：

- 水平翻转
- 垂直翻转
- 随机裁剪
- 中心裁剪
- 随机缩放裁剪
- 旋转
- 平移
- 缩放
- 剪切
- 透视
- 仿射
- 弹性形变
- 网格畸变
- 光学畸变
- Cutout / Random Erasing / Coarse Dropout
- Mixup / CutMix / Mosaic

原因是这些增强可能改变道路几何关系或遮挡关键车道线，从而导致图像语义和原始转向角标签不一致。

## 5. 新增 val_stress 作为主要选模指标

### 5.1 为什么需要 val_stress

普通 `val` 集来自同一数据集随机划分，往往和训练集风格非常接近。因此即使普通验证误差较低，也不一定说明模型能适应陌生环境。

为了更接近实际问题，本次新增 `val_stress`：

```text
val 原图 -> 固定强风格 photometric 增强 -> 验证模型稳定性
```

`val_stress` 的特点：

- 使用验证集图像，不参与训练。
- 只做安全的图像风格增强。
- 使用固定随机种子，保证每个 epoch 看到的 stress 验证图一致。
- 用于模拟陌生环境中的光照、曝光、颜色和画质变化。

### 5.2 检查点选择方式变化

以前模型通常按普通验证集指标保存最佳权重，例如：

```text
best_val_mae
best_val_loss
```

现在主线模型优先按：

```text
val_stress_mae
```

或分类链中的：

```text
val_stress_angle_mae
```

保存最佳模型。

这样做的目的是：

- 不再只选择“最适合同分布验证集”的模型。
- 优先选择对风格扰动更稳定的模型。
- 更贴近实际陌生环境部署需求。

## 6. 回归主线调整

对应目录：

```text
E:\桌面\项目\e2e_self-driving\Net
```

### 6.1 模型结构调整

原始回归模型是从零训练的小型卷积网络。它在小数据集上容易记住当前地图、当前光照、当前背景纹理，因此陌生环境泛化较弱。

本次将回归主线改为：

```text
MobileNetV3-Small 轻量骨干 + 回归头 + 辅助分类头
```

新增共享骨干文件：

```text
E:\桌面\项目\steering_models.py
```

对应模型文件：

```text
E:\桌面\项目\e2e_self-driving\Net\models.py
```

### 6.2 为什么使用预训练骨干

ImageNet 预训练骨干已经学习到通用视觉特征，例如：

- 边缘
- 纹理
- 光照变化
- 局部形状
- 颜色变化
- 低层视觉结构

相比从零训练的小网络，预训练骨干在小数据集场景下通常更不容易过拟合当前场地背景。

这对陌生环境泛化尤其重要。

### 6.3 双头训练目标

回归主线现在输出两个结果：

1. 连续转向角预测。
2. 辅助角度类别预测。

损失函数为：

```text
SmoothL1(angle_pred, angle_gt)
+ 0.3 * CrossEntropy(aux_logits, angle_class)
```

默认：

```text
VENET_AUX_CLS_WEIGHT=0.3
```

这样设计的原因是：

- 真实数据集中唯一转向角数量很少，任务本身具有明显的“离散控制”特征。
- 单纯连续回归容易在陌生环境下产生离谱偏差。
- 辅助分类头可以让模型学习角度所属控制区域，提高预测稳定性。
- 主输出仍然是连续角度，不改变回归主线的部署语义。

### 6.4 两阶段训练

训练分两阶段：

```text
前 5 个 epoch：冻结 backbone，只训练 head
后续 epoch：解冻 backbone，整体微调
```

默认配置：

```text
VENET_FREEZE_BACKBONE_EPOCHS=5
VENET_BACKBONE_LR_FACTOR=0.1
```

这样做的原因是：

- 刚开始训练时，避免小数据集直接破坏预训练特征。
- 先让任务头适配当前转向角任务。
- 再用较小学习率微调 backbone，提高任务适应性。

### 6.5 保留非均匀采样

回归主线继续保留基于转向角分布的 `WeightedRandomSampler`。

原因是数据集中直行和转向样本比例不均衡。如果不处理，模型容易偏向多数样本，从而对转弯样本泛化不足。

## 7. 分类残差主线调整

对应目录：

```text
E:\桌面\项目\e2e_competition\Net_class
```

### 7.1 保留 9 个硬编码原型

本次没有修改你已经统计得到的 9 个角度原型。

继续保留：

```text
9 类原型分类 + residual 小残差回归 + grouped soft decode
```

原因是：

- 9 个原型来自真实运行数据统计，有实际依据。
- 分类残差结构能限制离谱偏差。
- residual 头可以补偿相邻角度之间的小差异。

### 7.2 替换为轻量预训练骨干

旧分类模型同样是小型卷积网络，从零训练时容易依赖当前地图纹理。

现在 `Net_class` 也切换到共享轻量骨干：

```text
MobileNetV3-Small 风格 backbone + 9 logits + 1 residual
```

对应文件：

```text
E:\桌面\项目\e2e_competition\Net_class\models.py
```

### 7.3 删除几何增强

旧训练中存在类似：

```text
RandomAffine
```

这类增强会改变道路、车道线和视角关系，但没有同步修正转向角标签，因此会破坏标签一致性。

本次已删除几何增强，只保留安全 photometric 增强。

### 7.4 类别频次加权

分类损失加入类别频次权重：

```text
CrossEntropy(weight=class_weights, label_smoothing=0.05)
```

原因是：

- 转向角类别分布不均衡。
- 频次高的类别不应该完全主导训练。
- 频次低的转弯类别需要被模型充分学习。

## 8. Net_improve 调整

对应目录：

```text
E:\桌面\项目\e2e_competition\Net_improve
```

`Net_improve` 本轮不再作为主攻路线，而是作为 A/B 对照线保留。

调整内容包括：

- 删除几何增强。
- 使用相同的全图 HSV 预处理。
- 使用轻量预训练骨干。
- 新增 `val_stress_angle_mae`。
- 按 stress 验证指标选择 checkpoint。

这样做的目的不是继续堆第三条复杂路线，而是让它在公平条件下作为参考模型存在。

如果后续实验中它仍然明显落后，就可以不再作为最终部署候选。

## 9. Web 训练与推理调整

### 9.1 Web 推理对齐

Web 端推理文件：

```text
E:\桌面\项目\e2e_self-driving\Net_Web\app\services\inference.py
```

现在已改为复用共享预处理：

```text
全图 -> HSV -> Resize(120,160) -> Tensor
```

并且支持新模型与旧模型 checkpoint 的兼容加载。

### 9.2 Web 训练对齐

Web 端训练工作流：

```text
E:\桌面\项目\e2e_self-driving\Net_Web\app\training_runner.py
```

已同步调整：

- 使用安全增强。
- 使用新回归双头训练目标。
- 记录 `val_stress`。
- 训练结果中回填 `valStressMAE`。
- 分类模型和 improve 模型也解析 stress 指标。

### 9.3 Web 训练入口对齐

`Net_Web/train.py` 现在直接转发到主回归训练入口，避免维护两份逐渐分叉的训练脚本。

对应文件：

```text
E:\桌面\项目\e2e_self-driving\Net_Web\train.py
```

## 10. 联调脚本调整

联调脚本：

```text
E:\桌面\项目\joint_infer_compare.py
E:\桌面\项目\joint_infer_dataset_compare.py
```

现在默认：

```text
全图推理
ROI=False
```

如果需要启用 ROI 诊断，需要显式加参数：

```powershell
--enable-roi --roi-bottom-ratio 0.6
```

这样做是为了避免默认联调结果和正式训练/部署契约不一致。

## 11. TensorRT 实车推理预处理调整

文件：

```text
E:\桌面\项目\e2e_self-driving\trtCrun.py
```

已改为调用共享预处理逻辑：

```text
全图 -> HSV -> Resize(120,160) -> float32 / 255 -> CHW
```

这保证训练、Python 推理和 TensorRT 推理在输入张量语义上尽量一致。

## 12. 兼容旧权重

为了避免以前训练好的权重完全无法加载，本次在模型文件中保留了 legacy 回退：

- `e2e_self-driving/Net/models.py`
- `e2e_self-driving/Net_Web/models.py`
- `e2e_competition/Net_class/models.py`
- `e2e_competition/Net_improve/models.py`

加载 checkpoint 时会优先尝试新模型结构。如果发现是旧权重，则回退到旧模型结构。

这对继续比较新旧模型非常重要。

## 13. 新增训练摘要字段

训练完成后，摘要中会增加或强化以下字段：

```text
modelSelectionMetric
finalValStressMAE
finalValStressAngleMAE
pretrainedLoaded
usePretrained
freezeBackboneEpochs
preprocess
angleVocab
```

其中最关键的是：

```text
modelSelectionMetric = val_stress_mae
```

这表示当前最优模型不再只由普通验证集决定，而是由强风格验证表现决定。

## 14. 为什么这些调整更适合当前实际应用

当前实际应用的核心难点是：

- 单张固定地图训练数据有限。
- 转向角类别高度离散。
- 真实部署环境可能出现陌生背景、陌生光照、不同曝光、不同地面材质和不同相机画质。
- 小模型从零训练容易记住当前场地纹理，而不是学习真正稳定的视觉控制特征。

本次调整分别对应这些问题：

| 问题 | 调整 |
|---|---|
| 训练/推理不一致 | 统一共享预处理契约 |
| 小数据集容易过拟合 | 使用轻量预训练骨干 |
| 回归容易出现离谱偏差 | 回归主线加入辅助分类头 |
| 转向角分布不均衡 | 保留非均匀采样 / 类别频次权重 |
| 普通验证集不能反映陌生环境 | 新增 `val_stress` 并用它选模型 |
| 几何增强破坏标签 | 删除 RandomAffine 等几何增强 |
| 强风格域偏移明显 | 训练中加入 moderate/strong photometric 混合增强 |
| 部署不能大改 | 仍保持 120x160、HSV、轻量模型、单张输入 |

## 15. 后续评估建议

后续不建议只看普通随机测试集指标，而应至少同时看三组结果：

1. 普通 `test` 集。
2. 强风格测试集，例如 `E:\桌面\data1`。
3. 外部陌生环境 benchmark，例如 `E:\桌面\kunmingr2`。

建议重点比较：

```text
普通 test MAE
val_stress MAE
 data1 MAE
kunmingr2 分目录 MAE
```

如果普通测试集略有波动，但 `val_stress / data1 / kunmingr2` 稳定变好，那么这更符合当前“实际部署泛化优先”的目标。

## 16. 当前仍需注意的风险

这些调整可以提高泛化能力，但不能保证彻底解决陌生环境问题。主要原因是：

- 当前训练数据仍然主要来自单一地图或少量场景。
- 没有新增真实陌生环境标注数据。
- photometric 增强只能模拟光照/曝光/风格变化，不能完全模拟道路结构、背景布局和相机安装差异。
- 如果实车环境和训练环境几何分布差异很大，仅靠图像风格增强仍然有限。

因此，如果未来允许采集少量陌生环境数据，哪怕只标注少量关键场景，也会比继续盲目堆增强更有效。

## 17. 推荐下一步实验顺序

建议按以下顺序继续：

1. 使用 `E:\桌面\data` 训练三条新链路。
2. 分别测试普通 `test`、`data1`、`kunmingr2`。
3. 和旧模型结果并排比较。
4. 如果新模型普通测试略差但外部集明显更稳，优先考虑新模型。
5. 如果 `Net_improve` 仍然落后，则不再作为部署候选，只保留为对照。
6. 如果分类残差和回归双头各有优势，可以进一步考虑简单集成或按场景选择。

## 18. 总结

本次调整的核心不是简单“加大模型”或“加更多增强”，而是将整个项目训练目标从随机测试集表现转向实际陌生环境稳定性：

- 输入契约统一。
- 训练增强更贴近真实域偏移。
- 模型骨干更适合小数据泛化。
- 回归任务加入离散控制先验。
- 选模指标改为更能反映环境变化的 `val_stress`。
- Web、联调、TensorRT 推理链路同步对齐。

这套调整更符合当前项目的实际应用背景：在不大改部署、不新增陌生环境标注数据的情况下，尽可能提高模型对陌生环境的泛化能力和推理稳定性。
