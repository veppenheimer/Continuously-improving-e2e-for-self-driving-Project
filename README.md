# e2e_competition Archive

`Net_class` and `Net_improve` were removed from the active project on 2026-04-21.

This directory is now an archive for historical notes only. It no longer contains runnable training pipelines.

The maintained training and Web entry variants are:

- `Legacy CNN`
- `单帧 MobileNetV2`
- `3-frame`
