### 底盘控制

roslaunch eprobot_start AIcar_joy.launch

### 手柄启动

roslaunch eprobot_joy eprobot_ps2_control.launch #手柄

### yolo启动（后文有锁定线程的启动方式）

cd /opt/nvidia/deepstream/deepstream-6.0/sources/DeepStream-Yolo

deepstream-app -c deepstream_app_config.txt #yolo启动

### 新功能包编译

catkin_make -DCATKIN_WHITELIST_PACKAGES="yolo_sign_msg" --pkg yolo_sign_msg #功能包编译

### trt模型板端转换

/usr/src/tensorrt/bin/trtexec --onnx=results/0606x_simplified.onnx --saveEngine=test/0606x_simplified_fp16.engine --fp16 

### 锁定板端频率

sudo jetson_clocks --show

sudo jetson_clocks

### yolo启动（锁定线程，其他文件也可采用，nano四核心可酌情分配）

taskset -c 2 deepstream-app -c deepstream_app_config.txt

### 系统信息查询：

sudo tegrastats
