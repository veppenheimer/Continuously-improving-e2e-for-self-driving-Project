# 后端 API 根地址（不要末尾斜杠）
# 开发时若使用 Vite 代理，可设为 /api，并在 vite.config 里配置 VITE_API_PROXY_TARGET
VITE_API_BASE_URL=http://127.0.0.1:8000

# 可选：开发代理目标（仅当 VITE_API_BASE_URL=/api 时由 vite 转发）
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# 可选：WebSocket 训练进度（若后端提供 ws://）
# VITE_WS_BASE_URL=ws://127.0.0.1:8000
