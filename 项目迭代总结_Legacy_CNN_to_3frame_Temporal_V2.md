# 项目迭代总结：从 Legacy CNN 到 3-frame Temporal V2

## 1. 项目背景与初始问题

本项目面向端到端自动驾驶小车控制任务，目标是根据单目摄像头图像直接预测车辆转向角。整体场景具有较强的竞赛任务特征：车辆主要运行在固定地图、固定或近似固定轨迹上，转向角集合相对有限，但实际部署时仍会受到光照、曝光、画面风格、摄像头角度和运行速度等因素影响。

早期模型主要采用 `e2e_self-driving/Net` 中的 Legacy CNN 结构。该模型属于较典型的端到端单帧卷积回归模型：输入单张图像，经过卷积网络提取特征后直接输出连续转向角。在同源数据、随机划分测试集或与训练分布非常接近的场景中，Legacy CNN 可以取得较好的表现，说明它能够学习固定地图下的主要视觉线索。

但随着实际测试范围扩大，模型暴露出较明显的泛化问题，主要体现在以下几个方面：

- 对环境变化敏感：光照、亮度、对比度、曝光状态发生变化时，预测误差明显增大。
- 对陌生画面风格不稳定：即使地图主体相同，若画面上方背景、摄像头视角或显示状态变化，模型也容易输出偏差较大的转向角。
- 数据规模有限：源数据集覆盖了主要轨迹，但样本量不大，模型容易记住场景模板，而不是真正学习稳定的赛道结构。
- 采样分布不均衡：直行样本明显多于大转角样本，模型容易形成偏保守预测。
- 单帧输入信息不足：车辆速度较快时，连续两帧图像差异可能不大，但控制标签会从直行突然跳到较大转角，单帧模型难以判断是否需要提前转向。
- 转向角分布离散：实际数据中唯一转向角数量有限，任务既有回归属性，也具有一定的离散控制特征。
- 训练与推理契约曾不完全统一：不同模型链路中存在输入尺寸、颜色空间、ROI、增强方式不一致的问题，影响了实验可比性和部署可靠性。

因此，后续优化并不是单纯追求随机测试集指标，而是围绕“如何提升实际运行中的鲁棒性与泛化能力”展开。整个项目的核心目标逐渐明确为：在尽量不显著增加推理资源消耗的前提下，提高模型面对光照变化、画面风格变化和短时控制跳变时的稳定性。

## 2. 总体优化思路

整个项目的优化过程可以归纳为以下几条主线。

第一条主线是网络结构优化。早期 Legacy CNN 结构简单、从头训练，容易在小数据集上记住场景纹理。后续逐步引入轻量预训练骨干，例如 MobileNetV3-Small，并在回归头中加入更适合赛道几何信息的特征融合和空间注意力机制，使模型具备更强的特征抽取能力，同时仍控制推理成本。

第二条主线是时序信息建模。由于单帧图像无法稳定表达“车辆正在接近弯道”这一动态趋势，项目后期从单帧输入扩展到 3 帧短时序输入。该方向不是引入重型时序网络，而是采用轻量 3-frame temporal adapter，在不显著增加部署成本的情况下让模型获得短时间运动趋势信息。

第三条主线是数据集清洗与重构。为了避免随机划分造成相邻帧泄漏、指标虚高，以及训练/验证分布过于接近的问题，项目中逐步整理了数据集目录、划分文件、训练日志和测试结果，并将不同实验结果固定保存到规范目录中，使每一轮实验可以回溯和对比。

第四条主线是数据增强与鲁棒性增强。由于真实问题主要来自光照、亮度、曝光和画面风格变化，因此增强策略重点放在 photometric / image-quality 类安全增强上，例如亮度、对比度、gamma、局部曝光、轻微噪声、压缩、模糊等。同时明确避免会破坏转向语义一致性的几何增强，例如翻转、旋转、裁剪、透视变换等。

第五条主线是标签与采样策略优化。项目中针对转向角分布不均衡问题引入了基于角度分布的加权采样，使大转角样本在训练中获得更高采样概率。同时也尝试过标签前移，用于解决高速行驶下控制滞后的问题，但实验表明在当前单帧输入和当前视角下效果不理想，因此没有继续采用。

第六条主线是训练流程与损失设计改进。项目中加入了早停、验证集指标记录、强风格验证集 `val_stress`、EMA 权重、辅助分类头、软标签辅助监督、SmoothL1 回归损失等机制，使训练过程更加稳定，并且能够以“风格鲁棒性”而不是单纯随机验证集误差作为模型选择依据。

## 3. 迭代过程总览

| 阶段 | 版本/方案 | 主要改动 | 试图解决的问题 | 结果 |
|------|-----------|----------|----------------|------|
| 1 | Legacy CNN 单帧回归 | 使用早期端到端 CNN，单帧图像直接回归转向角 | 建立基础端到端控制模型 | 作为基线保留 |
| 2 | 分类模型 `Net_class` | 将转向角离散为硬编码原型类别，后续扩展为“原型分类 + 小残差回归 + 组内软解码” | 避免回归模型在环境变化时输出离谱偏差，提高离散控制稳定性 | 部分保留/作为辅助手段 |
| 3 | `Net_improve` 回归结构尝试 | 在回归方向上尝试改进网络结构 | 在不改变任务形式的情况下提升性能 | 效果一般，作为对照保留 |
| 4 | 安全数据增强与离线增强数据集 | 只使用亮度、对比度、gamma、曝光、噪声、压缩等安全增强，避免几何增强 | 缓解光照和画面风格变化带来的域偏移 | 部分保留/作为鲁棒性手段 |
| 5 | 强风格测试集 `data1` | 每张源图生成强风格扰动版本，用于测试模型对风格变化的稳定性 | 检查模型是否真正具备光照/曝光鲁棒性 | 保留为重要评测集 |
| 6 | 预处理统一 | 统一为全图、HSV、固定 Resize、默认无 ROI | 消除训练和推理不一致带来的误差 | 保留并沿用 |
| 7 | MobileNet 预训练回归主线 | 引入 MobileNetV3-Small 预训练骨干、辅助分类头、软标签、EMA、强风格验证 | 提升小数据集下的表征能力和风格泛化能力 | 保留并成为主线 |
| 8 | MobileNet V2 特征融合头 | 在 MobileNet 基础上加入中间特征融合和空间注意力池化 | 避免全局平均池化过早丢失赛道线空间位置信息 | 保留并沿用 |
| 9 | 标签前移 `Δ=1/2` | 将监督目标从 `image_t -> steering_t` 改为未来帧标签 | 解决高速行驶下入弯滞后和急弯提前量不足 | 效果有限，已弃用 |
| 10 | 3-frame Temporal V2 | 使用 3 帧堆叠输入和轻量时序适配器 | 缓解单帧歧义，利用短时连续信息提升稳定性 | 保留并作为当前最终版本 |

## 4. 各阶段尝试与取舍分析

### 4.1 Legacy CNN 单帧回归基线

#### 4.1.1 为什么要做这一步

项目最初需要建立一个可以端到端完成图像到转向角映射的基础模型。Legacy CNN 结构简单、训练成本低、部署推理开销小，适合作为早期基线模型。

在固定地图和固定轨迹场景下，该类模型能够较快学习到图像中车道线、道路边界和转向角之间的对应关系，因此最初采用 Legacy CNN 是合理的工程选择。

#### 4.1.2 具体改了什么

早期版本使用单帧输入，经过 CNN 卷积层提取特征，再通过全连接层输出连续转向角。整体训练目标以回归损失为主，输入主要经过固定尺寸 Resize 和颜色空间处理。

#### 4.1.3 这一步理论上解决什么问题

Legacy CNN 解决的是最基础的端到端建模问题：不再手写传统视觉规则，而是直接让模型从图像中学习转向控制量。它可以自动学习固定赛道中的视觉特征，例如车道线方向、赛道位置和弯道趋势。

#### 4.1.4 实际效果与判断

Legacy CNN 在同源数据或随机测试集上具有一定竞争力，说明它能够拟合固定地图下的主要控制规律。但它的缺点也比较明显：对环境变化和陌生画面风格非常敏感，尤其在外部测试集或光照变化较强的数据上容易出现明显退化。

这说明 Legacy CNN 的问题并不只是训练不足，而是其表征能力、鲁棒性机制和输入信息形式都比较有限。

#### 4.1.5 最终是否保留

Legacy CNN 最终不再作为部署主线，但作为重要基线模型保留，用于衡量后续模型是否真正带来泛化能力提升。

### 4.2 分类模型 `Net_class`

#### 4.2.1 为什么要做这一步

实际数据中的转向角并不是完全连续分布，而是集中在有限个转向角附近。早期回归模型在环境变化时可能输出偏离实际角度范围的异常值，因此曾考虑将任务从纯回归改为分类，以降低输出离谱偏差的风险。

此外，项目中已有 9 个硬编码转向角原型，这些原型来自实际运行图像转向角统计，不是随意设置的类别。

#### 4.2.2 具体改了什么

分类链路最初将转向角映射为离散类别，模型输出类别概率，再通过类别表得到转向角。后续进一步改进为：

- 保留 9 个硬编码角度原型。
- 不再把原始标签强行写回标准角度，而是保留原始浮点角度。
- 数据集中同时生成最近原型类别 `class_idx` 和类内残差 `delta_target`。
- 模型输出从纯分类扩展为 `9 logits + 1 residual`。
- 使用分类损失和残差回归损失联合训练。
- 推理时采用“原型分类 + 组内 soft 解码 + 小残差修正”的方式输出最终角度。

#### 4.2.3 这一步理论上解决什么问题

该方案试图同时保留分类模型的稳定性和回归模型的精度：

- 分类部分限制模型输出落在合理的转向原型附近，降低离谱预测风险。
- 残差部分补偿相近转向角之间的细微差别。
- 组内 soft 解码避免不同转向方向之间被错误平均。
- 对于 `1.51`、`1.52` 这类非常接近的角度，小残差回归可以提供更高精度。

#### 4.2.4 实际效果与判断

该方向在机制上是合理的，尤其适合转向角高度离散的竞赛场景。但后续测试表明，分类残差模型并没有成为综合表现最好的主线。它在部分情况下可以提高输出稳定性，但面对复杂风格变化和外部场景时，仍然受限于 backbone 表征能力和单帧输入信息不足。

#### 4.2.5 最终是否保留

`Net_class` 未作为最终部署主线，但其“离散原型 + 残差修正”的思想具有参考价值，目前作为辅助对照模型保留。

### 4.3 `Net_improve` 回归结构尝试

#### 4.3.1 为什么要做这一步

在分类方向之外，也需要保留纯回归优化路线。`Net_improve` 的目标是在不改变任务形式的情况下，通过调整网络结构提升回归性能，尽可能改善原始 CNN 的表达能力。

#### 4.3.2 具体改了什么

该阶段主要围绕回归网络结构进行优化，同时逐步对齐训练策略和预处理方式。它与分类模型不同，仍然保持连续转向角回归任务。

#### 4.3.3 这一步理论上解决什么问题

`Net_improve` 试图通过更强的网络结构提升图像特征表达能力，从而缓解 Legacy CNN 在环境变化下容易过拟合局部纹理的问题。

#### 4.3.4 实际效果与判断

从后续多轮实验结果看，`Net_improve` 的效果没有成为最优路线。它可以作为结构改造参考，但相比后来的预训练 MobileNet 主线和 3-frame temporal 方案，其泛化提升不够明显。

#### 4.3.5 最终是否保留

该尝试作为实验对照保留，但不再作为主要优化方向。

### 4.4 安全数据增强与强风格测试

#### 4.4.1 为什么要做这一步

由于实际部署中模型对光照、亮度、对比度、曝光变化敏感，因此需要通过数据增强让模型看到更多风格变化。但自动驾驶转向角回归任务对标签语义一致性要求很高，很多分类任务常用增强并不适合直接使用。

例如，水平翻转会改变左右转含义，随机裁剪可能丢失赛道关键区域，旋转、平移、透视变换会改变图像几何关系而不修改标签，都会造成图像与转向角标签不匹配。

#### 4.4.2 具体改了什么

项目中设计了较安全的传统图像增强策略，主要包括：

- 亮度扰动。
- 对比度扰动。
- gamma 变化。
- 轻微色彩扰动。
- 局部曝光变化。
- 轻微噪声。
- 轻微模糊。
- 图像压缩扰动。
- 轻微锐化。

同时明确避免以下增强：

- 水平翻转和垂直翻转。
- 随机裁剪、中心裁剪、随机缩放裁剪。
- 旋转、平移、仿射、透视、弹性形变等几何增强。
- Cutout、Random Erasing、Mixup、CutMix、Mosaic 等可能破坏局部语义或标签一致性的增强。

此外，还构造了强风格扰动数据集 `data1`，用于单独测试模型在强亮度、对比度、局部曝光变化下的表现。

#### 4.4.3 这一步理论上解决什么问题

安全 photometric 增强可以扩大训练样本在光照、曝光、颜色和图像质量方面的覆盖范围，从而降低模型对单一画面风格的依赖。强风格测试集则用于验证模型是否真正学习到了赛道结构，而不是只记住源数据的显示风格。

#### 4.4.4 实际效果与判断

单纯依靠离线数据增强并没有彻底解决泛化问题，部分模型在增强数据上训练后，整体效果提升有限，甚至在某些情况下不如原模型。这说明问题不仅仅是“增强不够多”，还包括模型结构、输入时序信息、训练选择指标和预处理一致性等因素。

但数据增强方向并未被否定。后续它被重构为更合理的在线风格混合增强，并与 `val_stress` 验证一起使用，成为训练鲁棒性的重要组成部分。

#### 4.4.5 最终是否保留

安全 photometric 增强和强风格测试集保留。过强或不安全的几何增强被弃用。

### 4.5 预处理统一与训练/推理一致化

#### 4.5.1 为什么要做这一步

在多模型、多脚本、多数据集实验过程中，如果训练、测试、Web 推理、联调脚本使用不同的输入处理方式，就会导致指标不可比，甚至让模型部署表现明显下降。

因此，需要先统一输入契约，避免模型在训练和实际推理时接收到不同分布的输入。

#### 4.5.2 具体改了什么

项目中逐步统一了主要模型链路的输入约定：

- 默认使用全图输入。
- 默认使用 HSV 颜色空间。
- 固定 Resize 尺寸。
- 默认不启用 ROI。
- 将预处理配置写入 checkpoint 或训练摘要。
- 推理脚本和联调脚本按 checkpoint 中的预处理配置执行。
- ROI 仅作为诊断开关，不作为默认部署策略。

早期 MobileNet 主线中曾使用 `HSV + 144x192 + Full Image`，Legacy CNN 则保持其原始契约用于公平对比。

#### 4.5.3 这一步理论上解决什么问题

预处理一致化可以减少训练和推理之间的分布偏移，保证模型评测结果更接近真实部署表现。同时，不同模型按各自 checkpoint 的预处理配置推理，也提高了多模型对比的公平性。

#### 4.5.4 实际效果与判断

该改动属于工程稳定性增强，不一定直接体现为单独指标提升，但它显著提高了实验可信度。后续所有模型评测、联调测试和多数据集对比都依赖这一机制。

#### 4.5.5 最终是否保留

该改动最终保留，并成为当前训练和评测链路的一部分。

### 4.6 MobileNet 预训练回归主线

#### 4.6.1 为什么要做这一步

Legacy CNN 从头训练，在小数据集上容易过拟合固定场景。为了提高表征能力和光照/风格鲁棒性，引入轻量 ImageNet 预训练骨干是一个高收益、低推理成本的方向。

MobileNetV3-Small 相比更重的 backbone 更适合部署在资源受限设备上，同时又比早期 CNN 具备更强的通用视觉特征提取能力。

#### 4.6.2 具体改了什么

该阶段引入了 MobileNetV3-Small 预训练骨干，并在回归任务中加入：

- 连续角度回归主头。
- 基于真实角度词表的辅助分类头。
- 邻域软标签辅助监督。
- SmoothL1 回归损失。
- EMA 权重。
- 加权采样。
- 风格混合在线增强。
- `val_stress` 强风格验证指标。
- 两阶段训练或可配置的 backbone 训练策略。

#### 4.6.3 这一步理论上解决什么问题

预训练 backbone 有助于模型学习更稳定的低级和中级视觉特征，降低小数据集从零训练时对特定纹理和光照的依赖。辅助分类头利用了转向角有限离散的特点，使模型在连续回归之外也能感知角度结构。软标签则避免把相近角度看成完全不同类别，更符合转向角连续性。

#### 4.6.4 实际效果与判断

MobileNet 主线相比 Legacy CNN 在外部场景和风格变化上表现更稳定。虽然在某些同源或随机测试场景中，Legacy CNN 曾表现较强，但从泛化角度看，预训练 MobileNet 主线更有继续投入价值。

#### 4.6.5 最终是否保留

MobileNet 预训练回归主线保留，并成为后续 V2 和 temporal 版本的基础。

### 4.7 MobileNet V2 特征融合与空间注意力

#### 4.7.1 为什么要做这一步

在早期 MobileNet 回归结构中，如果只使用最后一层特征并进行全局平均池化，可能会过早丢失赛道线的空间位置信息。而自动驾驶转向角预测不仅需要识别图像中有什么，还需要知道车道线位于画面中的什么位置、弯道趋势如何。

因此，需要在不显著增加 backbone 复杂度的情况下，增强模型对空间结构的感知能力。

#### 4.7.2 具体改了什么

MobileNet V2 回归头保留预训练 MobileNetV3-Small backbone，但在 head 中加入：

- 中间高分辨率特征接入。
- 最后一层语义特征接入。
- 轻量 `1x1` 降维。
- 上采样对齐。
- 深度可分离 `3x3` 融合。
- 空间注意力加权池化。
- 连续角度回归输出。
- 训练期辅助分类头。

#### 4.7.3 这一步理论上解决什么问题

中间特征保留更多空间细节，最终特征提供更强语义信息，二者融合有助于同时理解赛道局部几何和整体弯道趋势。空间注意力池化相比普通全局平均池化，可以让模型更关注与转向相关的区域，减少无关背景对输出的影响。

#### 4.7.4 实际效果与判断

MobileNet V2 成为后续主线的重要基础。在已有源数据和强风格测试集上，它相比早期方案具备更好的综合表现，也为后续 3-frame temporal 版本提供了稳定 backbone 和 head 结构。

#### 4.7.5 最终是否保留

该结构最终保留，并作为当前 3-frame Temporal V2 的核心单帧特征提取基础。

### 4.8 标签前移尝试

#### 4.8.1 为什么要做这一步

实际观察中，小车速度较快时，某些弯道会出现上一帧标签仍接近直行、下一帧标签突然变为大转角的情况。单帧模型面对这种数据时容易学到保守输出，因为当前图像差异不大，但标签变化很大。

因此，曾尝试将标签前移，让模型学习 `image_t -> steering_{t+Δ}`，希望提前预测即将需要的转向角。

#### 4.8.2 具体改了什么

项目中增加了数据集生成阶段的 `label_shift` 功能：

- 按图像序号排序。
- 使用未来第 `Δ` 帧的转向角作为当前图像标签。
- 记录 `labelShiftFrames` 到数据集摘要中。
- 分别尝试了 `Δ=2` 和 `Δ=1`。
- 由于 teacher 模型学习的是 `image_t -> steering_t`，在标签前移实验中关闭了 teacher distillation，避免监督错位。

#### 4.8.3 这一步理论上解决什么问题

标签前移可以补偿车辆速度、推理延迟和控制执行延迟，使模型更像是在做提前预判，而不是等到弯道已经非常明显时才输出大角度。

#### 4.8.4 实际效果与判断

实验结果表明，在当前视角和单帧输入条件下，标签前移效果不理想。`Δ=2` 明显放大了“图像还没明显变化但标签已经大跳”的矛盾，`Δ=1` 虽然比 `Δ=2` 好，但仍明显弱于 `Δ=0` baseline。

这说明当前问题不适合仅靠标签前移解决。根本原因在于当前单帧图像对未来控制量的可观测信息不足，如果没有更多中距离赛道预览或时序输入，前移标签反而可能制造更难学习的监督关系。

#### 4.8.5 最终是否保留

标签前移在当前阶段已弃用，最终回退到 `Δ=0`。

### 4.9 3-frame Temporal V2

#### 4.9.1 为什么要做这一步

标签前移实验说明，单纯修改监督目标并不能解决单帧信息不足问题。相比之下，引入短时序输入更直接：模型不仅看到当前帧，还能看到前两帧，从而判断画面变化趋势。

这与实际驾驶控制更一致，因为转向决策通常依赖连续视觉变化，而不是孤立单帧。

#### 4.9.2 具体改了什么

当前最终版本采用 3 帧堆叠输入：

- 输入由单帧变为连续 3 帧。
- 标签仍使用 `Δ=0`，即当前帧对应当前转向角。
- 使用 `frame_stride=1`。
- 数据集读取时自动查找同目录前序帧。
- 对多帧图像使用一致的数据增强和预处理。
- 增加图像缓存，避免三帧训练时重复磁盘读图。
- 模型加入轻量 temporal adapter。
- temporal adapter 将 9 通道堆叠输入轻量融合回 3 通道，再送入 MobileNet V2 主干。
- 评测脚本支持自动识别 temporal checkpoint，并在推理时回溯前两帧。

#### 4.9.3 这一步理论上解决什么问题

3 帧时序输入可以缓解单帧歧义：

- 看到车道线位置的连续变化。
- 看到车辆相对赛道中心的漂移趋势。
- 看到弯道逐渐接近的动态信息。
- 减少单帧偶然光照或纹理噪声对预测的影响。
- 对急转弯前的趋势更敏感。

轻量 temporal adapter 的作用是尽量利用多帧信息，同时避免直接把 MobileNet stem 改成重型 9 通道结构，从而更好地复用预训练 backbone 的视觉特征。

#### 4.9.4 实际效果与判断

3-frame Temporal V2 在当前实验中取得了明确正收益。相比单帧 MobileNet V2：

- `test_clean MAE` 从 `0.073537` 降至 `0.068950`。
- `data1 MAE` 从 `0.160019` 降至 `0.060090`。
- `kunmingr2 MAE` 从 `0.563371` 降至 `0.537475`。

这说明时序信息不仅提升了同源测试表现，也显著增强了强风格扰动下的稳定性，并且对外部数据也有小幅正向收益。

#### 4.9.5 最终是否保留

3-frame Temporal V2 最终保留，并作为当前阶段的最终版本和后续主线。

## 5. 失败或放弃的尝试总结

| 尝试项 | 原本想解决的问题 | 为什么没有继续采用 | 当前结论 |
|--------|------------------|--------------------|----------|
| 纯分类转向角预测 | 限制输出范围，避免回归模型在环境变化时输出离谱角度 | 纯分类难以处理相近角度之间的高精度差异，例如相近的大转角 | 不作为最终主线，分类残差思想保留为参考 |
| 分类残差模型作为主模型 | 同时获得分类稳定性和残差精度 | 综合表现未超过 MobileNet 回归主线，仍受单帧输入和表征能力限制 | 作为辅助对照保留 |
| `Net_improve` 独立回归结构优化 | 在回归任务上提升 Legacy CNN 表现 | 相比预训练 MobileNet 主线收益不够明显 | 作为实验对照保留 |
| 大规模离线增强直接替换训练集 | 通过增加风格变化提升泛化 | 单纯增加增强样本没有稳定提升，部分情况下甚至影响同源性能 | 不作为单独解决方案，改为在线风格混合增强 |
| 几何类增强 | 扩大图像分布覆盖 | 会破坏图像与转向角标签的一致性，例如翻转、旋转、裁剪会改变控制语义 | 明确弃用 |
| ROI 作为默认输入策略 | 减少无关背景干扰 | 当前部署目标强调统一全图输入，ROI 可能引入训练/推理不一致，也可能丢失中距离赛道趋势 | 仅保留为诊断开关 |
| 标签前移 `Δ=2` | 解决高速入弯滞后 | 前移过大，使当前图像难以解释未来大转角标签，验证和测试表现明显退化 | 弃用 |
| 标签前移 `Δ=1` | 用较小提前量改善控制滞后 | 虽然比 `Δ=2` 好，但仍明显弱于 `Δ=0` baseline | 弃用，回退到 `Δ=0` |
| 直接 9 通道改 MobileNet stem 的时序版 | 让模型直接接收 3 帧堆叠输入 | 直接改 stem 会削弱预训练权重复用效果，训练表现不稳定 | 替换为轻量 temporal adapter |
| 继续单纯加大 backbone | 提升特征表达能力 | 会增加推理资源消耗，不符合小车部署约束，且问题核心并非单纯模型容量不足 | 暂未采用 |

## 6. 当前最终版本：3-frame Temporal V2

### 6.1 输入层面

当前最终版本从单帧输入升级为 3 帧短时序输入。模型不再只依赖当前一张图像，而是同时接收当前帧和前两帧。

这一改动主要解决单帧输入信息不足的问题。在实际小车高速运行时，连续两帧画面差异可能很小，但转向角可能已经发生明显变化。单帧模型只能根据当前图像做判断，容易输出保守的中间值。3 帧输入则可以让模型看到赛道线相对位置的变化趋势，从而更好判断车辆是否正在接近弯道、是否需要更积极转向。

### 6.2 主干特征提取层面

相比早期 Legacy CNN，当前版本的主干特征提取能力有明显升级：

- 使用 MobileNetV3-Small 预训练骨干。
- 保留轻量化部署优势。
- 引入中间高分辨率特征和最终语义特征融合。
- 使用 `1x1` 降维和深度可分离卷积进行轻量融合。
- 使用空间注意力加权池化代替单纯全局平均池化。
- 保留训练期辅助分类头，增强角度结构感知。

这些结构改动使模型不仅能识别赛道相关视觉元素，还能更好保留车道线空间位置、弯道趋势和局部几何信息。

### 6.3 时序建模层面

3-frame Temporal V2 的核心思想是：不引入复杂时序网络，而是用低成本方式让模型获得短时间视觉变化信息。

具体来说，模型将 3 帧图像堆叠后，通过一个轻量 temporal adapter 进行时序融合，再送入 MobileNet V2 主干。这样做有几个优点：

- 比单帧模型更容易判断画面变化趋势。
- 比 RNN、Transformer 等复杂时序模型更轻量。
- 可以尽量复用预训练 backbone。
- 对部署成本影响较小。
- 对连续控制任务更自然。

在赛道控制中，转向角不仅取决于当前画面，还取决于车辆相对赛道的位置变化趋势。时序信息可以减少单帧偶然噪声和视觉歧义，使预测更加稳定。

### 6.4 数据与预处理层面

当前版本在数据和预处理方面进行了系统整理：

- 使用统一数据集目录和训练结果目录。
- 使用固定划分文件进行训练、验证和测试。
- 使用 HSV 颜色空间增强光照鲁棒性。
- 使用固定 Resize。
- 默认全图输入，不启用 ROI。
- 训练阶段使用安全 photometric 在线增强。
- 验证和测试阶段只使用确定性预处理。
- 使用 `data1` 作为强风格扰动评测集。
- 使用 `kunmingr2` 作为外部场景护栏测试集。

增强策略只保留不会破坏转向标签语义一致性的内容，例如亮度、对比度、gamma、局部曝光、轻微噪声、轻微压缩和轻微模糊。几何增强、翻转、裁剪、透视变换等均被避免。

这些处理主要缓解光照变化、曝光变化和画面风格变化造成的分布偏移。

### 6.5 标签与采样策略层面

当前版本回退到 `Δ=0` 标签策略，即当前帧对应当前转向角。标签前移尝试被弃用，因为在当前视角和数据条件下，前移标签会使监督关系变得更难学习。

同时，模型保留了基于转向角分布的加权采样策略。由于直行样本明显多于大转角样本，如果完全按原始分布训练，模型容易偏向保守预测。加权采样通过按角度分桶或按绝对转向角分布计算 sample weight，提高转弯样本在训练中的出现概率，从而缓解样本分布不均衡问题。

### 6.6 训练策略层面

当前版本使用了一系列训练稳定性策略：

- SmoothL1Loss 作为主要回归损失。
- 辅助分类头使用真实角度词表和邻域软标签。
- 加权采样缓解直行样本占比过高。
- 在线风格混合增强提升光照和曝光鲁棒性。
- `val_stress` 强风格验证用于选择更鲁棒的模型。
- EMA 权重用于提升最终权重稳定性。
- 早停机制和最佳 epoch 记录用于减少过拟合风险。
- 训练日志、摘要和评测结果统一保存，方便复现实验。

这些策略使模型训练不再只关注随机验证集误差，而是更重视真实部署中可能遇到的风格变化和鲁棒性问题。

### 6.7 最终版本的核心优势

当前 3-frame Temporal V2 的核心优势可以概括为：

- 对环境变化更稳健：强风格扰动集 `data1` 上相比单帧 MobileNet V2 有明显改善。
- 对短时连续场景理解更合理：模型能够利用前序帧判断赛道趋势，而不是只依赖当前帧。
- 对单帧偶然噪声更不敏感：时序输入降低了单帧光照、纹理或局部异常对预测的影响。
- 对大转角和急弯场景更有潜力：短时序信息有助于识别入弯趋势，缓解单帧保守预测。
- 工程可用性更强：模型仍保持轻量，未引入重型 backbone 或复杂时序模块。
- 实验链路更完整：训练、推理、评测、日志和报告都已规范化，便于后续继续迭代。

## 7. 从 Legacy CNN 到 3-frame Temporal V2 的总体对比

| 对比维度 | Legacy CNN | 3-frame Temporal V2 | 改进意义 |
|----------|------------|---------------------|----------|
| 输入形式 | 单帧图像 | 连续 3 帧图像 | 引入短时序信息，缓解单帧歧义 |
| 特征提取能力 | 从头训练的简单 CNN | 预训练 MobileNetV3-Small + 特征融合头 | 提高小数据集下的视觉表征能力 |
| 时序信息利用 | 不利用时序 | 使用 3-frame temporal adapter | 捕捉赛道线和车辆相对位置变化趋势 |
| 数据处理方式 | 基础预处理 | 统一 HSV、固定 Resize、全图输入、checkpoint 记录预处理 | 减少训练与推理不一致 |
| 鲁棒性增强 | 较弱 | 在线安全 photometric 增强 + `val_stress` | 提升光照、曝光和画面风格鲁棒性 |
| 样本分布处理 | 原始采样为主 | 基于角度分布的加权采样 | 缓解直行样本过多导致的保守预测 |
| 损失设计 | 主要回归损失 | SmoothL1 回归 + 软标签辅助分类 | 同时利用连续角度和离散角度结构 |
| 模型选择指标 | 更偏常规验证集 | 引入强风格验证指标 | 选出更适合真实部署变化的模型 |
| 对环境变化适应性 | 对陌生风格较敏感 | 对强风格扰动和外部测试更稳定 | 泛化能力更强 |
| 工程完整性 | 基础训练与推理 | 数据划分、日志、评测、报告、联调脚本体系化 | 更便于复现、比较和继续迭代 |
| 部署成本 | 低 | 略高但仍保持轻量 | 在可接受资源范围内换取鲁棒性提升 |

## 8. 整个迭代过程的结论

整个项目的优化过程不是简单堆叠模型模块，也不是盲目调参，而是围绕“泛化性能不足”这一核心问题持续定位、提出假设、实验验证并筛选保留。

项目最初从 Legacy CNN 单帧回归模型出发，发现其在同源数据上可以工作，但面对环境变化和画面风格变化时不够稳定。随后分别尝试了分类模型、分类残差模型、回归结构改进、安全数据增强、强风格测试、预处理统一、MobileNet 预训练 backbone、空间特征融合、标签前移和短时序建模等方向。

其中，有些尝试没有成为最终方案，例如标签前移和直接 9 通道改 stem 的时序结构；有些尝试虽然不是主线，但提供了重要参考，例如分类残差模型和 `Net_improve`；有些机制则最终沉淀到当前版本中，例如预处理统一、安全增强、加权采样、强风格验证、MobileNet 特征融合头和 3 帧轻量时序输入。

当前 3-frame Temporal V2 并不一定是最终终点，但它相比初始 Legacy CNN 已经具备更强的解释性、鲁棒性和工程合理性。它不仅提高了模型对风格扰动和外部场景的稳定性，也说明项目已经从“训练一个能跑的模型”演进到“围绕真实部署问题构建完整实验闭环”。

这一路迭代的价值不只在于最终模型指标，更在于形成了一套清晰的方法论：发现问题、分析原因、提出改进假设、设计实验验证、根据结果保留或放弃方案，并最终沉淀出更可靠的工程系统。
