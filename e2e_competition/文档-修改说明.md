# e2e_competition 分类模型改造说明

本文档说明当前 `e2e_competition/Net_class` 的高精度分类链路。此链路仅修改分类模型相关代码，不改动 `Net_improve`；`Net_improve` 仍用于回归任务的结构优化实验。

## 目标

在保留 9 个硬编码转向角原型不变的前提下，避免“纯回归在单地图场景外推偏差过大”和“纯九分类只能跳档输出”的问题。当前方案采用：

- 原型分类：先判断属于哪一个原型类别。
- 类内残差回归：学习相对该原型的小偏移量。
- 组内软解码：只在同一转向组内做 softmax 加权，避免跨组平均。

这样既保留分类任务的稳定性，又让 `1.51`、`1.52` 这类接近角度在推理时能连续变化，而不是被硬压成同一个固定档位。

## 九个原型角度

`Net_class/steering_config.py` 中保留以下 9 个原型角度，顺序即类别索引 `0` 到 `8`：

| 类别索引 | 原型角度 |
|---------|---------|
| 0 | 1.72 |
| 1 | 1.64 |
| 2 | 1.50 |
| 3 | 0.00 |
| 4 | -1.50 |
| 5 | -1.56 |
| 6 | -1.58 |
| 7 | -1.60 |
| 8 | -1.62 |

分组固定为：

- 正转组：`[0, 1, 2]`
- 直行组：`[3]`
- 负转组：`[4, 5, 6, 7, 8]`

## 当前实现

### 1. 数据列表保留原始角度

`Net_class/create_data_lists.py` 现在会把文件名中的原始浮点转向角直接写入 `train.txt` / `val.txt`，不再提前量化成九个标准值。

示例：

```text
/path/to/img_1.51.jpg 1.51
/path/to/img_1.52.jpg 1.52
/path/to/img_-1.58.jpg -1.58
```

### 2. 数据集同时生成类别与残差

`Net_class/datasets.py` 从原始角度生成：

- `class_idx`：最近原型类别
- `delta_target`：`raw_angle - class_center[class_idx]`

同时对残差做截断：

- `MAX_DELTA = 0.04`

也就是说，模型只学习每个原型附近一个很小的连续偏移，避免残差头把输出拉得过远。

### 3. 模型输出为 9 类 logits + 1 个残差

`Net_class/models.py` 的最终输出维度为 `NUM_CLASSES + 1`，即：

- 前 9 维：分类 logits
- 最后 1 维：残差 raw delta

因此当前分类模型的输出张量形状为 `(N, 10)`。

### 4. 训练采用联合损失

`Net_class/train.py` 使用联合损失：

- 分类损失：`CrossEntropyLoss(label_smoothing=0.05)`
- 残差损失：`SmoothL1Loss`
- 总损失：`cls_loss + 2.0 * reg_loss`

日志与 TensorBoard 记录以下指标：

- `CE_Loss`
- `Val_CE_Loss`
- `Train_Total_Loss`
- `Val_Total_Loss`
- `Train_Acc`
- `Val_Acc`
- `Train_Angle_MAE`
- `Val_Angle_MAE`

其中 `Angle_MAE` 是最终解码角度与原始角度之间的绝对误差，能直接反映真实转向精度。

### 5. 推理解码逻辑

统一解码逻辑位于 `Net_class/steering_config.py`：

1. 用 logits 找到全局置信度最高的类别。
2. 根据该类别确定所属转向组。
3. 只在该组内部做 softmax 加权，得到 `base_angle`。
4. 残差头输出经 `tanh(raw_delta) * MAX_DELTA` 映射为 `delta`。
5. 最终输出：`final_angle = base_angle + delta`。

这样可以保证：

- 单峰类别输出时，结果接近对应原型角。
- 同组相邻类别共同激活时，结果可在原型之间连续变化。
- 不会出现“正转与负转一起平均”的跨组异常。
- 残差始终限制在 `±0.04` 内。

### 6. ONNX 与测试脚本

以下脚本都已经切换到统一解码逻辑：

- `Net_class/test.py`
- `Net_class/onnx_inference.py`
- `Net_class/export_onnx.py`

导出的 ONNX 仍然保持单输出张量，部署端不需要额外拼接多输出结构，只需复用同样的解码规则即可。

## Net_Web 接线

`e2e_self-driving/Net_Web` 已接入新的分类模型链路：

- `app/routers/tasks.py`
  - 分类模型推理不再使用 `argmax -> 固定角度表`
  - 改为动态加载 `Net_class/steering_config.py` 中的 `decode_output()`
- `app/training_runner.py`
  - `competitionClass` 训练日志会解析 `Train_Angle_MAE` / `Val_Angle_MAE`
  - `competitionClass.steeringError` 现在表示真实角度误差
- `app/training_artifacts.py`
  - 会从 TensorBoard 中恢复分类模型的角度 MAE

`competitionLite` 仍走 `Net_improve` 自己的链路，不受这次分类改造影响。

## 使用说明

### 1. 生成数据列表

在 `Net_class` 中准备图片后，按原项目命名规则组织图片文件名，例如：

```text
xxx_1.51.jpg
xxx_1.52.jpg
xxx_-1.58.jpg
```

运行：

```powershell
cd e2e_competition/Net_class
python create_data_lists.py
```

默认会扫描 `./data/1_1` 并生成：

- `data/1_1/train.txt`
- `data/1_1/val.txt`

注意：生成的列表中保留的是原始浮点角度，不再提前量化。

### 2. 训练分类模型

可直接运行：

```powershell
cd e2e_competition/Net_class
python train.py
```

也可通过环境变量覆盖训练参数：

```powershell
$env:VENET_DATA_FOLDER = "E:/your_dataset_root"
$env:VENET_BATCH_SIZE = "32"
$env:VENET_EPOCHS = "100"
$env:VENET_LR = "0.0001"
$env:VENET_OUTPUT_DIR = "E:/your_output_dir"
$env:VENET_SAVE_NAME = "ve2.pth"
$env:VENET_LOG_DIR = "E:/your_output_dir/runs"
python train.py
```

### 3. 导出与推理

导出 ONNX：

```powershell
python export_onnx.py
```

单图测试：

```powershell
python test.py path/to/image.jpg -c ve2.pth
```

ONNX 推理：

```powershell
python onnx_inference.py
```

无论是 PyTorch、ONNX 还是 Web 端比较推理，当前都应走同一套 `decode_output()` 逻辑。

## 已完成的烟雾验证

本轮已用一个最小数据集完成以下检查：

- `Net_class/train.py` 可在 1 个 epoch 下正常训练并输出 `(N, 10)` 结构权重。
- `Net_Web/app/training_runner.py` 能正确解析分类日志与 `Angle_MAE`。
- `Net_Web/app/training_artifacts.py` 能从 TensorBoard 恢复 `competitionClass.steeringError`。
- `Net_Web/app/routers/tasks.py` 能动态加载 `Net_class` 并执行新的分类解码。

## 注意事项

- 旧版九分类权重若只输出 9 维 logits，则与当前 `9 + 1` 输出头不兼容，需要重新训练。
- 当前方案默认超参数：
  - `MAX_DELTA = 0.04`
  - `label_smoothing = 0.05`
  - `reg_loss_weight = 2.0`
- 本次改造不涉及 `Net_improve`，也不修改 ROS / TensorRT / 实车控制脚本。